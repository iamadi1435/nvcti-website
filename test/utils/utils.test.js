// We can test the utility functions here
// I've made one test

// Pre-loaded modules
const { join } = require('path')

// NPM modules
const chai = require('chai')
const { expect } = chai
const chaiHttp = require('chai-http')
